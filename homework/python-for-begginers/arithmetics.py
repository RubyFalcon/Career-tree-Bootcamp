# Python requires us to import math module to do complex calculations
import math #if u want to know  all methods, look up python3 math module
# math is now an object in our python file so we can use . notation to access its methods 
# in python we have ints(full nums) and floats(decimal numbers)
# the operators in python are +-*/ : the basics; then we have ** exponent and // floor division, then we have % modulo

# Augmented assignment operator
# x = 10
# we want to increment by 3
# x = x +3 
# or we can  shorthand it
# x += 3
# these both are the same : we can also use -= /= *= 

# if we want to inc by 1 we can use ++

# Math Functions:

x = 2.9
# round:
print(round(x))  # 3.0 - rounds to nearest whole number
# abs:
print(abs(-2.9)) # 2.9 - gives the absolute value (positive representation of a number)

print(math.ceil(2.9)) # 3.0 ceil: rounds the number up

print(math.floor(2.9)) #2.0 floor: rounds down

# whenever we want 

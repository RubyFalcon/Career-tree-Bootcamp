# in python we have ints(full nums) and floats(decimal numbers)
# the operators in python are +-*/ : the basics; then we have ** exponent and // floor division, then we have % modulo

# Augmented assignment operator
x = 10
# we want to increment by 3
x = x +3 
# or we can  shorthand it
x += 3
# these both are the same : we can also use -= /= *= 

# if we want to inc by 1 we can use ++

